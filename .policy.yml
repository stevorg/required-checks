policy:
  approval:
    - tests passed

approval_rules:
  - name: tests passed
    requires:
      conditions:
        has_workflow_result:
          conclusions: ["success", "skipped"]
          workflows:
            - ".github/workflows/component-a.yaml"
            - ".github/workflows/component-b.yaml"
