name: CI

on:
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Fake test
        run: |
          sleep 15
          exit 0
  release:
    if: false
    runs-on: ubuntu-latest
    steps:
      - name: Fake release
        run: |
          exit 0
